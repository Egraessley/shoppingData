import { NgxErrorDirective } from './ngxerror.directive';

describe('NgxerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxErrorDirective(null);
    expect(directive).toBeTruthy();
  });
});
