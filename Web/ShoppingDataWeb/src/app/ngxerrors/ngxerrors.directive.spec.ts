import { NgxErrorsDirective } from './ngxerrors.directive';

describe('NgxerrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxErrorsDirective(null);
    expect(directive).toBeTruthy();
  });
});
